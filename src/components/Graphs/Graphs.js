import React from 'react';
import PropTypes from 'prop-types';
import { GraphsWrapper } from './Graphs.styled';

const Graphs = () => (
 <GraphsWrapper>
    Graphs Component
 </GraphsWrapper>
);

Graphs.propTypes = {};

Graphs.defaultProps = {};

export default Graphs;
