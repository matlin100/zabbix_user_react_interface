import React from 'react';
import PropTypes from 'prop-types';
import { SidBarWrapper } from './SidBar.styled';

const SidBar = () => (
 <SidBarWrapper>
    SidBar Component
 </SidBarWrapper>
);

SidBar.propTypes = {};

SidBar.defaultProps = {};

export default SidBar;
